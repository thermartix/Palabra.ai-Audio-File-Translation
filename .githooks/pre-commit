#!/bin/sh
set -eu

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "gitleaks is required for secret scanning. Install it before committing." >&2
  exit 1
fi

gitleaks git --staged --redact --no-banner
